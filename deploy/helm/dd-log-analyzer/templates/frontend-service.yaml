apiVersion: v1
kind: Service
metadata:
  name: {{ include "dd-log-analyzer.fullname" . }}-frontend
  labels:
    {{- include "dd-log-analyzer.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dd-log-analyzer.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
